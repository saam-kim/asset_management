# 자산관리 활동 검토 — 2026-09-04

## 검토 방법

실제 학생 참여 조사 대신, 로컬 Chromium에서 교사 1명·학생 3명의 화면을 조작하는 자동 체험을 진행했다. 학생마다 목표 2개 작성 → 예산 설정 → 25~60세 시뮬레이션 → 결과 확인을 완료했다. 운영 Firebase에 테스트 기록을 남기지 않도록 외부 네트워크를 차단하고 로컬 체험 모드를 사용했다.

| 체험 | 선택 | 확인한 점 |
| --- | --- | --- |
| 절약형 | 월급 300만 원, 월 적립 50만 원, 기대수익률 3% | 목표 비용 추산과 예산 설정, 60세 결과 |
| 균형형 | 월급 300만 원, 월 적립 100만 원, 기대수익률 6% | 납입액이 다른 학생의 결과를 단순 순위로 비교하면 안 됨 |
| 적자 조정형 | 월급 100만 원에서 시작, 적립 50만 원, 기대수익률 9% | 적자일 때 진행 차단, 월급을 300만 원으로 바꾼 뒤 완료 |

개선 전 세 체험 모두 실행 오류는 없었지만, 고정 위험·고정 시장 경로에서 높은 기대수익률이 항상 유리했다. 개선 후에는 A/B/C 세 경로를 나눠 재검증했다. 월급 조정은 테스트를 위한 조건 변경이지 현실에서 소득을 즉시 높일 수 있다는 뜻은 아니다. 수업에서는 지출·목표 시기 조정도 함께 토의한다.

## 학생 관점 피드백과 반영

- **조건을 고치기 어려움:** 예산 수정 버튼을 추가하고 돌아갔을 때 적립액과 생활비 선택을 보존했다. 결과창의 다시 하기는 곧바로 재실행하지 않고 조건 선택 화면으로 돌아간다.
- **높은 기대수익률이 정답처럼 보임:** 기대수익률과 변동폭을 연결하고, 실제 평균을 기대수익률에 강제로 맞추던 계산을 없앴다. 비교용 시장 경로 3개를 제공한다. B에서는 기대 9%도 예적금보다 낮은 결과를 낸다.
- **목표 자금과 생애 자산 혼동:** 월 목표 적립액을 35년 비교 실험에 재사용하며 목표 비용은 인출하지 않는다고 명시했다. 비상금·세금·수수료·소득 변화 역시 실제 계산에는 포함되지 않는다.
- **AI·시세 안내 과장:** 기본값이 내장 예시이며 실시간 시세가 아니라고 수정했다.
- **설명 없는 결과 확인:** 선택 이유를 한 줄로 적을 수 있게 했다. 필수 제출은 아니다.
- **잘못된 기본 적립액:** 소액 목표가 0원으로 반올림되거나 큰 목표가 슬라이더 상한을 넘는 불일치를 수정했다. 권장액보다 적게 넣는 경우 안내한다.

## 교사 관점 피드백과 반영

- 월 적립액이 다른데 투자 격차만 보여 주던 목록에 적립액·적자 여부·기대수익률·시장 경로·완료 상태·선택 이유를 추가했다.
- 25분짜리 진행 순서와 토의 질문을 대시보드에 넣었다. 수익 순위 대신 조건과 선택 이유를 비교하도록 안내한다.
- 학생 이름·목표·선택 이유가 HTML로 해석되지 않도록 처리했다.
- 실행 중 수익률 변경을 막고 조건 수정 시 예전 결과와 완료 상태를 지워, 교사가 서로 다른 조건의 값을 같은 결과로 오해하지 않도록 했다.

## 검증과 한계

- `tests/classroom.cjs`: 3명 완료, 적자 차단, 교사 목록 반영, 설정 잠금, 예산 복귀·값 보존, 완료 상태, 선택 이유 전달, HTML 문자열 안전 출력, 결과창 화면 내 배치.
- `tests/model.cjs`: 원금, 월 복리 기준값, 동일 조건 재현, 경로별 차이, 최고 기대수익률의 예적금 하회 사례, 모든 경로의 유한·비음수 잔액.
- 외부 Firebase 연결·다른 실제 기기 재입장·배포 환경·QR 라이브러리 로드는 이번 테스트 범위가 아니다. 실제 학생의 이해도나 교육 효과를 검증한 것도 아니다.
- 시장 경로는 비교를 위해 선정한 가상 예시다. 과거 통계, 확률 분포의 대표 표본, 상품 추천, 수익 예측으로 사용하면 안 된다.
- 목표 시점의 인출, 부채 상환, 실직, 세금 등을 실제 계산에 넣는 확장은 아직 하지 않았다. 교사 공유용 자료의 난이도를 유지하기 위해 이번에는 계산 범위를 분명히 표시했다.

## 테스트 실행

Node.js와 Playwright가 필요하다. Chromium 실행 파일 위치는 `tests/classroom.cjs` 상단에서 환경에 맞게 조정한다.

```powershell
node tests/model.cjs
$env:IMPROVED='1'
node tests/classroom.cjs
```

Playwright가 프로젝트 밖에 설치되어 있다면 해당 `node_modules` 경로를 `NODE_PATH`에 지정한다. UI 테스트는 임시 로컬 서버를 열고 테스트가 끝나면 닫는다. 결과·교사 화면 PNG는 시스템 임시 폴더의 `asset-management-report.png`, `asset-management-teacher.png`에 저장한다.
